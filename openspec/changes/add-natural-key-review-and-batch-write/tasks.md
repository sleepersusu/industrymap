## 1. 公司對外識別統一（F2，先做因為後續自然鍵會用到）

- [x] 1.1 先寫失敗測試：有主要代號的公司，`SupplierResponse.companyReference` 應回代號而非正規化名稱
- [x] 1.2 先寫失敗測試：無任何識別碼的公司應退回正規化名稱
- [x] 1.3 抽出共用的公司對外識別組裝邏輯，`CompanyResponse` 與 `SupplierResponse` 改用同一套
- [x] 1.4 先寫失敗測試：任一回應取出的對外識別拿去 `GET /api/companies/{code}` 都查得到；再確認實作滿足

## 2. 自然鍵定位型別

- [x] 2.1 建立各資料類型的自然鍵 payload（節點、別名、組成關係、公司、識別碼、供應角色、市佔率）
- [x] 2.2 `ReviewTarget` / `ReviewRequest` 新增自然鍵欄位，`targetId` 改為非必填
- [x] 2.3 先寫失敗測試：id 與自然鍵皆未提供應回 400；再以 `@AssertTrue` 實作邊界驗證
- [x] 2.4 補齊 `@Schema` 說明各類型的自然鍵組合

## 3. 自然鍵解析（design D2）

- [x] 3.1 先寫失敗測試：以 `(identifierType, identifierValue)` 應解析到對應的公司識別碼；再實作解析元件
- [x] 3.2 先寫失敗測試：以 `(parentItemId, childItemId)` 應解析到對應的組成關係
- [x] 3.3 先寫失敗測試：以正規化名稱應解析到節點別名與公司別名
- [x] 3.4 先寫失敗測試：以 `(公司識別, 零件, 角色)` 應解析到供應角色
- [x] 3.5 先寫失敗測試：以完整六維度應解析到市佔率
- [x] 3.6 先寫失敗測試：市佔率缺少期間或地區時應回 400，且訊息指出缺哪些維度；再實作缺漏檢查
- [x] 3.7 先寫失敗測試：自然鍵查無對應資料應回 404
- [x] 3.8 補齊 repository 依自然鍵查詢的方法（缺者才補，既有可用者沿用）

## 4. 審核端點接上自然鍵

- [x] 4.1 先寫失敗測試：以自然鍵審核公司識別碼應成功，且全程不需內部 id；再接上 `ReviewApplyService`
- [x] 4.2 先寫失敗測試：同時提供 id 與自然鍵時應以 id 為準，不回錯誤
- [x] 4.3 先寫失敗測試：批次中混用 id 與自然鍵應逐筆各自解析；再接上 `BatchReviewService`
- [x] 4.4 更新 `ReviewController` 的 OpenAPI 註解，說明兩種定位方式

## 5. 組成關係查詢端點

- [x] 5.1 先寫失敗測試：查詢節點的組成關係應回上下層節點、必要性與審核狀態；再實作端點
- [x] 5.2 先寫失敗測試：明確指定納入草稿時應含草稿、任何情況都不含已駁回
- [x] 5.3 補 OpenAPI 註解與 Controller 測試

## 6. 批次建立（F4）

- [x] 6.1 建立批次請求／回應的共用 payload，回應每筆帶成功與否、失敗原因、成功時帶自然鍵
- [x] 6.2 先寫失敗測試：空批次應回 400；再實作驗證
- [x] 6.3 先寫失敗測試：批次建立品類節點，其中一筆名稱重複時其餘應成功、該筆回報 409
- [x] 6.4 先寫失敗測試：同批次內兩筆名稱相同時，第一筆成功、第二筆回報 409
- [x] 6.5 實作品類節點、組成關係、公司、識別碼、供應角色、市佔率的批次建立端點
- [x] 6.6 先寫失敗測試：失敗項目的回應不得帶定位資訊；再確認實作滿足
- [x] 6.7 **關鍵驗收**：先寫失敗測試——批次建立後直接把回應轉為批次審核請求應全部成功，過程中不需任何額外查詢

## 7. 端到端重跑與收尾

- [x] 7.1 清空資料庫後，以新的批次端點重跑一台腳踏車的種子資料，記錄實際呼叫次數與上次對比
- [x] 7.2 驗證公司識別碼這一類確實可經 API 審核完成，全程不查資料庫（F1 的驗收）
- [x] 7.3 更新 `docs/insomnia/industrymap.insomnia.json`：加入批次端點與自然鍵審核請求，移除上次標註的已知問題註記
- [x] 7.4 記錄本次仍未解決或新發現的可用性問題，寫入 design.md 供 AI pipeline change 參考
- [x] 7.5 執行 `./mvnw clean test-compile` 後 `./mvnw surefire:test` 確認快測全綠
- [x] 7.6 執行 `./mvnw verify` 跑全量，附實際輸出
- [ ] 7.7 以 `/code-review` 進行 fresh-context diff review，只修正確性問題
- [x] 7.8 更新 `.claude/rules/api-design.md`：補上「內部作業端點可用自然鍵定位」與「不曝露內部 id 原則的適用範圍」
- [x] 7.9 更新 `docs/CHANGELOG.md`（含 `companyReference` 的行為變更）
